## Лабораторна робота з дисципліни "Алгоритми та структури даних"

## Виконала: Держипільська Анастасія Андріївна (ІР-14)
## Лабораторна робота №1 (Варіант 6)
## Task №1

## Створіть змінну 'car' та присвойте їй назву вашої улюбленої автомобільної марки. Виведіть рядок, який містить назву марки.
## Створіть змінну `is_student` та присвойте їй значення `True`, а змінну `is_teacher` - значення `False`. Виведіть результати логічних операцій
## Створіть змінну `is_red` та присвойте їй значення `True`, а змінну `is_blue` - значення `True`. Виведіть результати логічного "або".           


car = "Range Rover"
print("Моя улюблена автомобільна марка", car)

is_student = True
is_teacher = False

print("is_student AND is_teacher:", is_student and is_teacher)
print("is_student OR is_teacher:", is_student or is_teacher)
print("NOT is_student", not is_student)

is_red = True
is_blue = True

print("is_red OR is_blue", is_red or is_blue)
